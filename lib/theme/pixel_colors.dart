import 'package:flutter/material.dart';

class PixelColors {
  static const Color background = Color(0xFF1A468D); // Deep Blue from the door
  static const Color primary = Color(0xFFFDE047);    // Heart Yellow
  static const Color secondary = Color(0xFF2459B0);  // Medium Blue
  static const Color accent = Color(0xFFEF4444);     // Brick Red
  static const Color surface = Color(0xFF1E3A8A);    // Darker Blue for containers
  static const Color text = Color(0xFFFFFFFF);
  static const Color border = Color(0xFFFFFFFF);     // White for graffiti style
  static const Color textSecondary = Color(0xFFD1D5DB);
  static const Color canvas = Color(0xFFF0E68C);
  
  // Palette for 8-bit hero
  static const Color heroSkin = Color(0xFFFFDBAC);
  static const Color heroTunic = Color(0xFF32CD32);
  static const Color heroBelt = Color(0xFF8B4513);
}
